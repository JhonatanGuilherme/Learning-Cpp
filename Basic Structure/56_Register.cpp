#include <iostream>

using namespace std;

int main() {
  // Register - Requisição ao sistema para armazenar
  // a variável no registrador do processador, o que
  // torna o processo mais rápido.
  // Obs: O compilador quem decide.
  register auto cont = 0;

  return 0;
}
