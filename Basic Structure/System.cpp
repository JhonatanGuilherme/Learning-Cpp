#include <iostream>
#include <stdlib.h>

using namespace std;

int main() {
  system("dir"); // Retorna diretório do arquivo original;
  system("cls"); // Limpa a tela;
  system("color f0"); // Altera as cores do terminal;
  system("pause"); // Pausa a execução do arquivo;

  return 0;
}
